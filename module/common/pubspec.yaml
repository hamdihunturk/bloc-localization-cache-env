name: common
description: 'A new Flutter package project.'
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.4
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

  #network
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0

flutter:
